version: '3.9'

services:
  postgres: 
    image: postgres:15
    container_name: developerstore-postgres
    environment:
      POSTGRES_DB: developerstore
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: _senha_dever@
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    restart: always

  rabbitmq:
    image: rabbitmq:3-management
    container_name: developerstore-rabbitmq
    ports:
      - "5672:5672"
      - "15672:15672" # UI
    restart: always

volumes:
  postgres_data:
  